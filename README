<h1>aoeu.sh</h1>

This is a shell alias sets, design for Linux / MacOS users, especially for Dvorak keyboard layout users.

<h2>Core alias</h2>

<pre>
alias a='ls'
alias aa='ls | grep'
alias o='cd ..'
alias e='vim'
alias u='pwd'
alias i='cd'
alias m='mkdir'
alias d='rm -r'
alias f='find -name'
alias q='exit'
alias c='clear'
alias t='tail -f'
alias p='ps aux | grep'
alias h='e /etc/hosts'
alias x='tar zxvf'
</pre>

<h2>Surfing in directories</h2>


<pre>
cd download
ls
cd ..
l
